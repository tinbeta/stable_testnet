{
  "name": "esc_app-contracts",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "clean": "rimraf artifacts cache",
    "compile": "hardhat compile",
    "compile-and-copy": "npm run compile && node scripts/copyArtifacts.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.10",
    "rimraf": "^5.0.5"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  }
}

